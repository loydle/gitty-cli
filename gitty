#!/bin/bash
echo

# add all changes
git add -v .

if [ $@ ~ ""  ]; then 
	printf "message: "
	read msg
	echo
	# commit 
	git commit -m "$msg"
else 
	# commit 
	git commit -m "$@"
fi
# push
git push

echo

